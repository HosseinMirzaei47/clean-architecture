<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="title"
            type="String" />

        <variable
            name="count"
            type="Integer" />

        <variable
            name="clickListener"
            type="com.speaking.partner.ui.utils.EpoxyActionListener" />

    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center">

        <com.google.android.material.card.MaterialCardView
            android:id="@+id/cv_category_item"
            onSafeClick="@{clickListener::onAction}"
            android:layout_width="@dimen/_75sdp"
            android:layout_height="@dimen/_75sdp"
            android:layout_marginVertical="@dimen/margin_micro"
            app:cardCornerRadius="@dimen/_15sdp"
            app:cardElevation="@dimen/elevation_small">

            <androidx.constraintlayout.widget.ConstraintLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:layout_gravity="center"
                android:layout_margin="@dimen/margin_small">

                <ImageView
                    android:id="@+id/iv_all_tasks_status"
                    android:layout_width="0dp"
                    android:layout_height="@dimen/_25sdp"
                    android:contentDescription="@string/todo"
                    android:scaleType="fitStart"
                    android:src="@drawable/ic_iconly_light_work"
                    app:layout_constraintEnd_toStartOf="@+id/txt_all_tasks_count"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintTop_toTopOf="parent"
                    app:tint="@color/matisse" />

                <TextView
                    android:id="@+id/txt_all_tasks_count"
                    style="@style/TextAppearance.ToDo.AllCategories.Item.Count"
                    android:layout_width="0dp"
                    android:layout_height="0dp"
                    android:gravity="center"
                    android:text="@{count.toString()}"
                    app:layout_constraintBottom_toBottomOf="@+id/iv_all_tasks_status"
                    app:layout_constraintEnd_toEndOf="parent"
                    app:layout_constraintStart_toEndOf="@+id/iv_all_tasks_status"
                    app:layout_constraintTop_toTopOf="parent"
                    tools:text="12" />

                <TextView
                    style="@style/TextAppearance.ToDo.AllCategories.Item.Title"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@{title}"
                    app:layout_constraintBottom_toBottomOf="parent"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintTop_toBottomOf="@+id/iv_all_tasks_status"
                    tools:text="Personal" />
            </androidx.constraintlayout.widget.ConstraintLayout>
        </com.google.android.material.card.MaterialCardView>

    </LinearLayout>
</layout>